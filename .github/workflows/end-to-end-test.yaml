name: end-to-end


on:
  push:
    branches:
      - main
 

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: print-github
        run: |
          AUTHOR='${{ github.triggering_actor }}'
          MESSAGE="The author is $AUTHOR"
          echo "$MESSAGE"
          # echo "::set-output name=author::$AUTHOR"
          echo "author=$AUTHOR" >> $VAR_AUTHOR
 
      - name: print
        run: |
          echo '${{ steps.print-github.outputs.VAR_AUTHOR }}'
          echo '${{ VAR_AUTHOR }}'
        
      - uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.DISPATCH_TOKEN }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'dispatch-a',
              workflow_id: 'ci.yaml',
              ref: 'main',
              inputs: {
                origin: context.repo.repo,
                author: '${{ steps.print-github.outputs.author }}'
              }
            })    

